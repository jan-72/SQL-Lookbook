delimiter $$$
create procedure sumn (n int)
begin
declare i int;
declare sums int;
set sums=0;
set i=1;
while i<=n do
    set sums=sums+i;
    set i=i+1;
end while;
select sums;
end $$$

call sumn(10);

