delimiter $$$
create procedure sumn (n int)
begin
declare i int;
declare sums int;
set sums=0;
set i=1;
outerloop: while i<=n do
    if i%3=0 or i%5=0 then 
         set sums=sums+i;
    end if;
    set i=i+1;
end while outerloop;
select sums;
end $$$

call sumn(10);
