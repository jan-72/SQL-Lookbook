delimiter $$$
create procedure seriessum()
begin
declare k float;
declare m float;
set k=1;
set m=0;
while k<=100000 do
    set m = m+4*(power(-1,k+1)/(2*k-1));
    select m;
    set k=k+1;
end while;
select m;
end $$$

call seriessum();
