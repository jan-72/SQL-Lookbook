delimiter $$$
create procedure leapyear (n int)
begin
declare i int;
declare j int;
set i=year(now());
set j=1;
while j<=n do
    if i%4=0 and i%100<>0 then select i;
    end if;
    set j=j+1;
    set i=i+1;
    end while;
end $$$

call leapyear(2);
