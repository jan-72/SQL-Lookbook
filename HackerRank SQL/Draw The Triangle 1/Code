delimiter $$$
create procedure stars (in rown int)
begin
declare n int;
set n=rown;
      while n>0 do
        select repeat("* ",n) as line;
        set n=n-1;
      end while;
end$$$

delimiter ;
call stars(20);
