with cte as 
(
    select x, y,row_number() over(order by x) as rown from functions
)

select distinct f1.x, f1.y  from cte as f1
join cte as f2 on f1.x=f2.y and f1.y=f2.x and f1.rown<>f2.rown
where f1.x<=f1.y 
order by f1.x
